// this will be the design template for the future modular system
void apply_dns_system(const char *dns, int is_dhcp){
    char cmd[256];
    if(is_dhcp){
        #if defined (_WIN32)
            //
        #else
            //
        #endif
    }
}